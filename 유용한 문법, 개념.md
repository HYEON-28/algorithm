# 알고리즘 유용한 문법, 개념

## string 오름차순 정렬

Char배열로 바꾼다음, 정렬 후 다시 string으로 변환

char[] arr = str.toCharArray();
Arrays.sort(arr);
String sorted = new String(arr);

// String sorted = arr.toString() -> 주소값이

## Map에서 key로 반복문 실행

for(String key : map.keySet()) {
result.add(map.get(key));
}

## 소수

에라토스테네스의 체로 구분

## paiza용

```
Integer.parseInt() // string -> 정수

// 한줄의 string -> 숫자
String input = "5 2 3";
String[] arr = input.split(" ");
int a = Integer.parseInt(arr[0]);
int b = Integer.parseInt(arr[1]);
int c = Integer.parseInt(arr[2]);

```

```
switch(num) {
  case 1:
    break;

  default:

}
```

```
// 배열의 길이 구하기
arr.length // -> ()없음!
```

입력받기

```
opLevels[i] = Integer.parseInt(sc.nextLine());
int val = sc.nextInt(); -> 숫자가 한줄에 여러개 입력되어도 이거 사용!
```

문자열 포함여부
text.contains
문자열의 길이: text.length();

문자열 -> char배열
char[] arr = str.toCharArray();
